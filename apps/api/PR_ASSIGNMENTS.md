# PR Review Assignments

Branch: `team-consolidation`

## Stefanie
**Review:** Your `max_fairness_matching.py` implementation
**Finish:** Nothing - your implementation is complete! Just verify it works as expected.

## Joanna
**Review:** Your `white_elephant_simulation.py` implementation
**Finish (TODOs in code):**
- Implement "can't steal back immediately" rule
- Apply `we_hate_being_stolen_from` penalty to final happiness
- Ensure players can't open their own gift
- Consider non-linear happiness scaling

## Justin
**Review:** `random_matching.py` and `max_utility_matching.py` (basic implementations provided)
**Finish (TODOs in code):**
- `random_matching.py`: More efficient derangement algorithm, better exclusion handling
- `max_utility_matching.py`: Caching for large groups, edge case handling
- *Optional:* Refine `utility_calculator.py` weighting formula
